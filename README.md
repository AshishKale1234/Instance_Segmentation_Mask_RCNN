# Instance_Segmentation_Mask_RCNN
This project implements an end-to-end instance segmentation pipeline using Mask R-CNN with a ResNet50 + FPN backbone in PyTorch. The model is trained and evaluated on the COCO dataset, enabling pixel-level object detection and segmentation across multiple classes.
